# FP_PWeb

setup : 
```bash
composer install # setup dependency
composer update # setup dependency
npm run build # vite + tailwind init
php artisan key:generate # database init
php artisan migrate # database init
php artisan storage:link # for image uploading

php artisan serve # serve project
```

